@using Data.Entities
@model List<StreetRoad>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link href="~/Content/bootstrap.css" rel="stylesheet" />
    <title>Streets</title>
</head>
<body>
    <div class="container">
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col" class="text-center">Name</th>
                    <th scope="col" class="text-center">Length</th>
                    <th scope="col" class="text-center">Begin</th>
                    <th scope="col" class="text-center">End</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var element in Model)
                {
                    <tr>
                        <th scope="row">@element.Id</th>
                        <td class="text-center">@element.StreetName</td>
                        <td class="text-center">@element.StreetLenght</td>
                        <td class="text-center">@element.BeginOfStreet</td>
                        <td class="text-center">@element.EndOfStreet</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</body>
</html>


@*<table class="table table-striped table-hover">
        <thead>
            <tr>
                <th scope="col">Name</th>
                <th scope="col">Length</th>
                <th scope="col">Begin</th>
                <th scope="col">End</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var element in Model)
            {
                <tr>
                    <th scope="row">@element.Id</th>
                    <td class="text-center">@element.StreetName</td>
                    <td class="text-center">@element.StreetLenght</td>
                    <td class="text-right">@element.BeginOfStreet</td>
                    <td class="text-right">@element.EndOfStreet</td>
                </tr>
            }
        </tbody>
    </table>*@

@*@foreach (var el in Model)
{
    <div class="row">
        <div class="col-md-1">
            @el.Id
        </div>
        <div class="col-md-5">
            @el.StreetName
        </div>
        <div class="col-md-2">
            @el.BeginOfStreet
        </div>
        <div class="col-md-2">
            @el.EndOfStreet
        </div>
        <div class="col-md-2">
            @el.StreetLenght
        </div>
    </div>
}*@